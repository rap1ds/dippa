describe('Content', function() {

    it('should be able to create new instance', function() {
        var content = new Dippa.Content();
        expect(content).not.toBeNull();
        expect(content).not.toBeUndefined();
    });

})